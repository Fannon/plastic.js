/**
 * Validates the elData (Element Data) Object
 * Returns true if valid, false if not.
 *
 * TODO: Not implemented yet
 *
 * @param elData
 * @returns {boolean}
 */
plastic.validateElementData = function(elData) {

    console.info('plastic.validateElementData();');

    return true;
};